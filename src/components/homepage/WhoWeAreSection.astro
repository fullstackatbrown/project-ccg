---
const {statBoxes, clubDescription } = Astro.props;
---

<section id="who-we-are" class="w-full bg-gradient-to-b from-white to-[#F5F7FA] text-black py-24 overflow-hidden">
    <!-- Who Are We Section -->
    <div class="container mx-auto px-6 mb-24">
        <div class="flex flex-col md:flex-row items-start gap-12">
            <div class="md:w-1/3 mx-auto text-center mb-12 animate-element">
            <h1 class="text-5xl md:text-6xl font-bold mb-8 text-[#0B144A] text-center" style="font-family: 'arimo', sans-serif;">Who We Are</h1>
              <div class="flex items-center justify-center mb-8">
                    <div class="w-12 h-1 bg-gradient-to-r from-[#D32F2F] to-[#B71C1C]"></div>
                    <div class="w-3 h-3 rounded-full bg-gradient-to-r from-[#D32F2F] to-[#B71C1C] ml-1"></div>
                    <div class="w-12 h-1 bg-gradient-to-r from-[#D32F2F] to-[#B71C1C] ml-1"></div>
                </div>
            </div>
            <div class="md:w-2/3 slide-in-right delay-2">
                <p class="text-xl md:text-2xl font-light leading-relaxed text-gray-800">
                    Collegiate Consulting Group empowers students with <span class="font-medium text-[#D32F2F]">real-world consulting experience</span> through hands-on projects, workshops, and forums. Our external consulting projects expose students to each phase of the consulting process—from problem definition to final implementation—offering practical training and industry insights.
                </p>
            </div>
        </div>
    </div>
    
    <!-- Stats Section with enhanced visual appeal -->
    <div class="container mx-auto px-6">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10">
            {statBoxes.map((box, index) => (
              <div class={`relative bg-white p-8 rounded-lg shadow-xl text-center animate-element delay-${(index % 4) + 1} group hover:shadow-2xl transition-all duration-300 overflow-hidden`}>
                <div class="absolute inset-0 bg-gradient-to-br from-[#D32F2F]/5 to-[#B71C1C]/5 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                <div class="h-24 flex items-center justify-center relative">
                  <h3 class="text-6xl md:text-7xl font-bold bg-gradient-to-r from-[#D32F2F] to-[#B71C1C] bg-clip-text text-transparent mb-2 group-hover:scale-110 transition-transform duration-300">{box.value}</h3>
                </div>
                <p class="text-xl font-medium text-gray-700 mt-4 relative">{box.label}</p>
              </div>
            ))}
          </div>          
    </div>
</section>